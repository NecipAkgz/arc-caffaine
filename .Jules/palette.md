## 2024-05-23 - Non-Semantic Interactive Elements
**Learning:** The application uses `div` elements for interactive controls (e.g., scroll indicators) instead of semantic `button` or `a` tags, hindering keyboard navigation and screen reader support.
**Action:** Replace interactive `div`s with semantic HTML elements (`<button>`, `<a>`) and ensure proper ARIA attributes are present.
